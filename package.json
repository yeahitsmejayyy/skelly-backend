{
	"name": "@skelly/backend",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "bun run src/server.ts",
		"build": "bun build src/server.ts --outdir dist --target bun && cp src/db/schema.sql dist/schema.sql",
		"start": "bun run dist/server.js"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"@trpc/server": "^11.8.1",
		"zod": "^4.3.5"
	}
}