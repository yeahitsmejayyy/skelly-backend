{
	"name": "@skelly/backend",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "bun run src/server.ts",
		"build": "bun build src/server.ts --outdir dist --target bun && cp src/db/schema.sql dist/schema.sql && bun run types:emit",
		"start": "bun run dist/server.js",
		"types:emit": "bun scripts/emit-types.ts"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"@trpc/server": "^11.8.1",
		"zod": "^4.3.5"
	}
}